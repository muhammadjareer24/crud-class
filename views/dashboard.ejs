<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Dashboard</title>
  </head>
  <body class="bg-gray-100 min-h-screen">
    <header class="bg-blue-600 font-semibold p-4 text-white text-center">
      <h1 class="text-2xl">Dashboard</h1>
    </header>
    <main class="p-8 text-center">
      <p class="text-lg">Welcome to your dashboard! 👋</p>

      <ul class="my-10 text-lg">
        <% users.forEach((user)=> { %>
        <li>
          <span><%= user.fullName %></span>
          <a class="text-xs text-blue-600" href="/users/<%= user.id %>">View</a>
        </li>
        <% }) %>
      </ul>

      <div class="mt-6 space-x-4">
        <a
          href="/"
          class="px-4 py-2 bg-purple-500 text-white rounded transition-transform duration-300 hover:bg-purple-600"
          >Go to Home</a
        >
        <!-- <a
          href="logout.html"
          class="px-4 py-2 bg-red-500 text-white rounded transition-transform duration-300 hover:bg-red-600"
          >Logout</a
        > -->
      </div>
    </main>
  </body>
</html>
